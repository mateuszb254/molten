<!DOCTYPE html>

<title>TicketCategory</title>

{% block body %}
    <h1>TicketCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ticket_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ticket_category_index') }}">back to list</a>

    <a href="{{ path('ticket_category_edit', {'id': ticket_category.id}) }}">edit</a>

    {{ include('admin/ticket_category/_delete_form.html.twig') }}
{% endblock %}