{{ form_start(form, {'attr' : {'class': 'form-valide'}}) }}
<div class="form-group row">
    {{ form_label(form.coins, null, {'label_attr' : {'class': 'col-lg-4 col-form-label'}}) }}
    <div class="col-lg-6">
        {{ form_widget(form.coins, { 'attr' : { 'class' : 'form-control' } }) }}
    </div>
</div>
<div class="form-group row">
    {{ form_label(form.price, null, {'label_attr' : {'class': 'col-lg-4 col-form-label'}}) }}
    <div class="col-lg-6">
        {{ form_widget(form.price, { 'attr' : { 'class' : 'form-control'} }) }}
    </div>
</div>
<div class="form-group row">
    <div class="col-lg-8 ml-auto">
        <button type="submit" class="btn btn-primary">{% trans %}submit{% endtrans %}</button>
    </div>
</div>
{{ form_end(form) }}