<!DOCTYPE html>

<title>ShopProduct</title>

{% block body %}
    <h1>ShopProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shop_product.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ shop_product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shop_product_index') }}">back to list</a>

    <a href="{{ path('shop_product_edit', {'id': shop_product.id}) }}">edit</a>

    {{ include('admin/shop_product/_delete_form.html.twig') }}
{% endblock %}