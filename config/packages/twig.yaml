twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        project_name: '%project_name%'
        kingdom_extension_settings: '%kingdom_extension_settings%'
